## Build and run

```bash
sudo docker compose up --build
```

## Adminer

Open http://localhost:8080 with:

- Server: `mariadb`
- Username: `admin`
- Password: `password`
- Database: `uploads`

## Application

Open http://localhost:8081.
